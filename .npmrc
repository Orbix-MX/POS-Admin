node-linker=hoisted
# Acorta los nombres del virtual store (default 120). En Windows las rutas
# generadas por CMake/ninja para el build nativo de Android superaban MAX_PATH
# (263 chars en arm64-v8a) y el build fallaba con "build.ninja still dirty".
virtual-store-dir-max-length=40
