node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# Native builds
/ios
/android
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Secrets and per-developer config — never commit.
# `.env.development` holds a LAN address that differs per machine, so it is
# untracked too; `.env.example` stays in git as the documented template.
.env
.env.local
.env.development
.env.production
.env.development.local
.env.production.local

# Screenshots and diagnostic dumps taken while debugging on the emulator
# (adb screencap, uiautomator dump, gradle traces) — scratch, never commit.
.debug-captures/

# macOS
.DS_Store

# Metro / TS
*.tsbuildinfo
